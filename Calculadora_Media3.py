# O Roteiro da Resolução
# 1. Antes de sair digitando código, siga estes três passos:

# 2.Entenda a Entrada (Input): O que o programa recebe? (Ex: dois números).

# 3.Defina o Processamento: O que deve ser feito? (Ex: somar os números).

# 4.Determine a Saída (Output): O que o usuário deve ver? (Ex: o resultado da soma).

# Entrada
nota1 = float(input("Digite a primeira nota: "))
nota2 = float(input("Digite a segunda nota: "))

# Processamento
media = (nota1 + nota2) / 2

# Saída
print(f"A média final é: {media}")